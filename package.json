{
  "name": "it-is-what-is-it",
  "version": "1.0.0",
  "private": true,
  "description": "Real-time multiplayer drawing & guessing game — It is what is it?",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c magenta,cyan \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w shared && npm run build -w client && npm run build -w server",
    "start": "npm run start -w server",
    "typecheck": "npm run typecheck -w shared && npm run typecheck -w server && npm run typecheck -w client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.8.2",
    "vercel": "^58.7.1",
    "@railway/cli": "^4.5.2"
  },
  "engines": {
    "node": ">=20"
  }
}
