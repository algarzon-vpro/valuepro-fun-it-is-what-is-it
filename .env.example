# Client (set in Vercel project → Settings → Environment Variables)
# Absolute URL of the Socket.IO game server (no trailing slash)
VITE_SOCKET_URL=https://your-game-server.example.com

# Server (set on Railway / Render / Fly)
PORT=3001
CLIENT_ORIGIN=https://your-vercel-app.vercel.app
